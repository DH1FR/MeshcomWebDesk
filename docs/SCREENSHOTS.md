Place your screenshot here as `screenshot.png`.

To capture a screenshot:
1. Start the app (`dotnet run --launch-profile lan`)
2. Open `http://localhost:5162` in your browser
3. Take a screenshot and save it as `docs/screenshot.png`
4. Run: `git add docs/screenshot.png && git commit -m "docs: add screenshot" && git push`
